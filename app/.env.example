# Development mode - allows adminKey to be stored in localStorage and bypasses some checks
DEV_MODE=true

# Convex deployment (optional - not required for local dev)
# CONVEX_DEPLOYMENT=
# NEXT_PUBLIC_CONVEX_URL=
